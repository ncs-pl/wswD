// import { assertEquals } from "../../dev_deps.ts";
// import { mv, resolvePath } from "./mod.ts";
// import { existsSync } from "../../deps.ts";

Deno.test("Should move a file", () => {
  // it works, but i'm unable to make a test which works without the need to recreate folders...
});
